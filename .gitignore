.DS_Store
node_modules/

# Private skills kept local-only (this repo lives at ~/.claude/skills,
# so anything I do not want to publish is listed here and stays untracked).
agent-browser/
linear/
mac-cleanup/
pr:gen-linear/
